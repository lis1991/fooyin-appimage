name: Build AppImage from .deb (Universal)

on:
  schedule:
    - cron: '0 4 * * 0'  # Каждое воскресенье в 04:00 UTC
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-24.04
    permissions:
      contents: write

    steps:
      - name: 📥 Checkout Repository
        uses: actions/checkout@v4

      - name: 📦 Install System Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y wget curl jq python3-pip desktop-file-utils

      - name: 📦 Install AppImageBuilder (v1)
        run: |
          pip3 install --user appimage-builder==1.*

      - name: 🔨 Build AppImage (Auto-download .deb via script in AppImageBuilder.yml)
        run: |
          appimage-builder --recipe AppImageBuilder.yml --skip-test

      - name: 🐞 Debug - List files after build
        run: |
          echo "🔍 Содержимое текущей директории после сборки:"
          ls -la
          echo "🔍 Поиск файлов .AppImage:"
          find . -name "*.AppImage" -type f 2>/dev/null || echo "  Файлы .AppImage не найдены"

      - name: 📤 Upload AppImage Artifact
        uses: actions/upload-artifact@v4
        with:
          name: AppImage
          path: '*.AppImage'

      - name: 🏷️ Extract Version (Optional)
        id: get_version
        run: |
          FILE=$(find . -name "*.AppImage" -type f 2>/dev/null | head -1)
          if [ -z "$FILE" ]; then
            echo "::error::AppImage файл не найден после сборки!"
            echo "🔍 Доступные файлы в директории:"
            ls -la
            exit 1
          fi
          echo "✅ Найден AppImage файл: $FILE"
          echo "appimage_file=$FILE" >> $GITHUB_OUTPUT
          VERSION=$(date +"%Y%m%d%H%M%S")
          echo "🔖 Установлена версия (дата/время): $VERSION"
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      - name: 🚀 Create GitHub Release (Optional)
        if: success()
        uses: softprops/action-gh-release@v2
        with:
          files: ${{ steps.get_version.outputs.appimage_file }}
          tag_name: v${{ steps.get_version.outputs.version }}
          body: "Автоматическая сборка AppImage из последнего .deb-релиза через AppImageBuilder"
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
