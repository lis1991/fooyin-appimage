name: Сборка Fooyin AppImage (автоматически)

on:
  schedule:
    - cron: '0 4 * * 0'  # Каждое воскресенье в 04:00 UTC
  push:
    branches:
      - main
    tags:
      - 'v*' # Также собирать при новом теге (например, v1.2.3)

permissions:
  contents: write   # нужно для создания релизов

jobs:
  build-appimage:
    runs-on: ubuntu-24.04
    steps:
      - name: Клонировать репозиторий
        uses: actions/checkout@v4

      - name: Установить зависимости
        run: |
          sudo apt-get update
          sudo apt-get install -y wget curl jq binutils tar file libfuse2 \
            libglib2.0-0 libnss3 libnspr4 libatk1.0-0 \
            libatk-bridge2.0-0 libcups2 libdrm2 libdbus-1-3 \
            libexpat1 libfontconfig1 libgbm1 libgl1 \
            libgtk-3-0 libpango-1.0-0 libx11-6 libxcb1 \
            libxcomposite1 libxdamage1 libxext6 libxfixes3 \
            libxrandr2 libxrender1 libasound2t64 libatspi2.0-0 \
            libstdc++6


      - name: Скачать последний .deb пакет Fooyin
        id: download-deb
        run: |
          echo "🔍 Поиск последнего релиза Fooyin..."
          LATEST_TAG=$(curl -s "https://api.github.com/repos/fooyin/fooyin/releases/latest" | jq -r '.tag_name')
          DEB_URL="https://github.com/fooyin/fooyin/releases/download/${LATEST_TAG}/fooyin_${LATEST_TAG#v}-plucky_amd64.deb"
          echo "📥 Скачивание: $DEB_URL"
          wget -O fooyin_latest.deb "$DEB_URL"
          echo "LATEST_TAG=$LATEST_TAG" >> $GITHUB_ENV
          echo "DEB_URL=$DEB_URL" >> $GITHUB_ENV
          echo "✅ Успешно скачано: $LATEST_TAG"

      - name: 📦 Распаковка .deb файла
        run: |
          echo "📦 Распаковка .deb файла..."
          # Находим скачанный .deb
          DEB_FILE=$(ls *.deb | head -n 1)
          echo "Найден пакет: $DEB_FILE"

          # Распаковываем содержимое deb (ar-архив)
          ar x "$DEB_FILE"

          mkdir -p fooyin-appdir

          # Определяем, какой именно архив с данными внутри (.xz, .zst, .gz, ...)
          DATA_TAR=$(ls data.tar.* | head -n 1)
          echo "Распаковываем архив: $DATA_TAR"

          tar -xf "$DATA_TAR" -C fooyin-appdir/

          # Убираем мусор
          rm -f "$DEB_FILE" control.tar.* debian-binary "$DATA_TAR"


      - name: Скачать AppImageTool
        run: |
          wget https://github.com/AppImage/AppImageKit/releases/download/continuous/appimagetool-x86_64.AppImage
          chmod +x appimagetool-x86_64.AppImage

      - name: Создать структуру AppDir
        run: |
          mkdir -p AppDir/usr/bin
          mkdir -p AppDir/usr/lib
          mkdir -p AppDir/usr/share
          mkdir -p AppDir/usr/lib/x86_64-linux-gnu

      - name: Скопировать файлы из .deb в AppDir
        run: |
          cp -r fooyin-appdir/usr/* AppDir/usr/

          
      # -----------------------
      # Установка Qt 6.8.0 с помощью aqtinstall с кэшированием
      - name: Cache Qt 6.8.0
        id: cache-qt
        uses: actions/cache@v3
        with:
          path: /opt/Qt
          key: qt-${{ runner.os }}-6.8.0-gcc_64-${{ hashFiles('**/build.yml') }}
          restore-keys: |
            qt-${{ runner.os }}-6.8.0-gcc_64-

      - name: Install Qt 6.8.0 with aqtinstall
        id: install-qt
        run: |
          echo "=== Установка Qt 6.8.0 с помощью aqtinstall ==="

          # Определить версию Qt и архитектуру для aqtinstall
          QT_VERSION="6.8.0"
          # ВАЖНО: Используем 'gcc_64' как имя каталога, которое создает aqtinstall,
          # а не 'linux_gcc_64', которое возвращает команда list-qt
          QT_ARCH_DIR="gcc_64"
          # Имя архитектуры для команды aqt install-qt
          QT_ARCH_INSTALL="linux_gcc_64"
          
          QT_MODULES="qtmultimedia qt5compat qtshadertools"

          # Построить путь установки
          QT_INSTALL_PATH="/opt/Qt/$QT_VERSION/$QT_ARCH_DIR"
          
          if [ -d "$QT_INSTALL_PATH" ] && [ -f "$QT_INSTALL_PATH/bin/qmake6" ]; then
            echo "Qt $QT_VERSION ($QT_ARCH_DIR) найден в $QT_INSTALL_PATH (возможно из кэша или предыдущей установки). Пропуск установки aqtinstall."
            echo "cache-hit-local=true" >> $GITHUB_OUTPUT
          else
            echo "Qt $QT_VERSION ($QT_ARCH_DIR) не найден в $QT_INSTALL_PATH. Начинаем установку с помощью aqtinstall."
            echo "cache-hit-local=false" >> $GITHUB_OUTPUT

            echo "Установка aqtinstall..."
            python3 -m pip install aqtinstall

            echo "Создание каталога /opt/Qt и назначение прав..."
            sudo mkdir -p /opt/Qt
            sudo chown $USER:$USER /opt/Qt

            echo "Установка Qt $QT_VERSION ($QT_ARCH_INSTALL) с модулями: $QT_MODULES"
            INSTALL_CMD="aqt install-qt linux desktop $QT_VERSION $QT_ARCH_INSTALL -m $QT_MODULES -O /opt/Qt"
            echo "Команда установки: $INSTALL_CMD"
            if $INSTALL_CMD; then
              echo "Установка Qt $QT_VERSION с модулями '$QT_MODULES' прошла успешно."
            else
              echo "::warning::Установка Qt $QT_VERSION с модулями '$QT_MODULES' не удалась. Пробуем установить только базовый Qt без дополнительных модулей..."
              FALLBACK_CMD="aqt install-qt linux desktop $QT_VERSION $QT_ARCH_INSTALL -O /opt/Qt"
              echo "Команда установки (запасной вариант): $FALLBACK_CMD"
              if $FALLBACK_CMD; then
                echo "Установка базового Qt $QT_VERSION прошла успешно."
              else
                echo "::error::Критическая ошибка: Не удалось установить Qt $QT_VERSION ни с модулями, ни без них."
                echo "Доступные модули для Qt $QT_VERSION $QT_ARCH_INSTALL:"
                aqt list-qt linux desktop --modules $QT_VERSION $QT_ARCH_INSTALL || echo "Не удалось получить список модулей."
                exit 1
              fi
            fi
          fi

          # Проверить установку/наличие Qt
          echo "=== Проверка установленного/закэшированного Qt 6.8.0 ==="
          # Используем правильный путь QT_INSTALL_PATH, который указывает на gcc_64
          if [ -d "$QT_INSTALL_PATH/lib" ] && [ -f "$QT_INSTALL_PATH/bin/qmake6" ]; then
            echo "Каталог $QT_INSTALL_PATH/lib найден и qmake6 доступен."
            echo "Версия Qt (qmake6 -v):"
            $QT_INSTALL_PATH/bin/qmake6 -v || echo "Не удалось получить версию qmake6."
            echo "Наличие ключевых библиотек:"
            ls -la $QT_INSTALL_PATH/lib/libQt6Core.so* 2>/dev/null || echo "  libQt6Core.so* не найден"
            ls -la $QT_INSTALL_PATH/lib/libQt6Gui.so* 2>/dev/null || echo "  libQt6Gui.so* не найден"
            ls -la $QT_INSTALL_PATH/lib/libQt6Widgets.so* 2>/dev/null || echo "  libQt6Widgets.so* не найден"
            ls -la $QT_INSTALL_PATH/lib/libQt6Network.so* 2>/dev/null || echo "  libQt6Network.so* не найден"
            ls -la $QT_INSTALL_PATH/lib/libQt6DBus.so* 2>/dev/null || echo "  libQt6DBus.so* не найден"
            ls -la $QT_INSTALL_PATH/lib/libQt6Qml.so* 2>/dev/null || echo "  libQt6Qml.so* не найден"
            ls -la $QT_INSTALL_PATH/lib/libQt6Quick.so* 2>/dev/null || echo "  libQt6Quick.so* не найден"
            ls -la $QT_INSTALL_PATH/lib/libQt6Multimedia.so* 2>/dev/null || echo "  libQt6Multimedia.so* не найден"
            ls -la $QT_INSTALL_PATH/lib/libQt6OpenGL.so* 2>/dev/null || echo "  libQt6OpenGL.so* не найден"
            ls -la $QT_INSTALL_PATH/lib/libQt6Sql.so* 2>/dev/null || echo "  libQt6Sql.so* не найден"
            ls -la $QT_INSTALL_PATH/lib/libQt6Concurrent.so* 2>/dev/null || echo "  libQt6Concurrent.so* не найден"
            
            echo "Наличие ключевых плагинов (пример):"
            if ls $QT_INSTALL_PATH/plugins/platforms/libqxcb.so >/dev/null 2>&1; then
              echo "  Плагин XCB (libqxcb.so) найден"
            else
              echo "  Плагин XCB (libqxcb.so) не найден"
            fi
            if ls $QT_INSTALL_PATH/plugins/sqldrivers/libqsqlite.so >/dev/null 2>&1; then
              echo "  Плагин SQLite (libqsqlite.so) найден"
            else
              echo "  Плагин SQLite (libqsqlite.so) не найден"
            fi
          else
            echo "::error::Критическая ошибка: Каталог $QT_INSTALL_PATH/lib или qmake6 НЕ НАЙДЕН."
            echo "Содержимое /opt/Qt (если есть):"
            ls -la /opt/Qt/ 2>/dev/null || echo "  /opt/Qt пуст или не существует"
            echo "Содержимое /opt/Qt/$QT_VERSION/ (если есть):"
            ls -la /opt/Qt/$QT_VERSION/ 2>/dev/null || echo "  /opt/Qt/$QT_VERSION/ пуст или не существует"
            exit 1
          fi
          echo "=== Установка/проверка Qt 6.8.0 завершена ==="

      - name: Save Qt 6.8.0 Cache
        if: steps.cache-qt.outputs.cache-hit != 'true' && steps.install-qt.outputs.cache-hit-local == 'false'
        uses: actions/cache/save@v3
        with:
          path: /opt/Qt
          key: qt-${{ runner.os }}-6.8.0-gcc_64-${{ hashFiles('**/build.yml') }}
      # -----------------------         



      # -----------------------
      # Кэшируем сборку TagLib из Git
      - name: Cache TagLib Git Build
        id: cache-taglib
        uses: actions/cache@v3
        with:
          path: ~/taglib_build
          # Используем фиксированный ключ, пока не добавим динамическое определение версии
          key: taglib-git-tip-${{ runner.os }}

      # -----------------------
      # Сборка TagLib из Git, если кеш пустой
      - name: Build TagLib from Git
        if: steps.cache-taglib.outputs.cache-hit != 'true'
        run: |
          sudo apt-get update
          # Устанавливаем зависимости для сборки TagLib
          # cmake и g++ должны быть уже установлены, но перестрахуемся
          sudo apt-get install -y build-essential cmake git

          mkdir -p ~/taglib_build_source && cd ~/taglib_build_source
          # Клонируем репозиторий TagLib
          git clone https://github.com/taglib/taglib.git
          cd taglib
          git submodule update --init
          # Можно добавить git checkout <конкретный_стабильный_тег>, если нужно
          # Например: git checkout v2.0  (проверьте актуальный тег на GitHub)
          
          # Создаем директорию для сборки
          mkdir build && cd build
          # Конфигурируем сборку с помощью CMake
          # Устанавливаем префикс в ~/taglib_build
          # -DCMAKE_BUILD_TYPE=Release для оптимизированной сборки
          # -DBUILD_SHARED_LIBS=ON чтобы собирать shared libraries (.so)
          cmake .. -DCMAKE_BUILD_TYPE=Release -DCMAKE_INSTALL_PREFIX=$HOME/taglib_build -DBUILD_SHARED_LIBS=ON
          # Собираем
          make -j$(nproc)
          # Устанавливаем в ~/taglib_build
          make install

      # -----------------------
      # Копируем библиотеки TagLib в AppDir
      - name: Copy TagLib libs to AppDir
        run: |
          echo "=== Копирование библиотек TagLib из Git-сборки ==="
          if [ -d "$HOME/taglib_build/lib" ]; then
            echo "Найден каталог библиотек: $HOME/taglib_build/lib"
            mkdir -p AppDir/usr/lib
            # Копируем все .so файлы из каталога lib
            cp -v $HOME/taglib_build/lib/*.so* AppDir/usr/lib/
            
            echo "Файлы TagLib в AppDir/usr/lib/ после копирования:"
            ls -la AppDir/usr/lib/libtag* 2>/dev/null || echo "Файлы libtag* не найдены в AppDir/usr/lib/"
          else
            echo "::warning::Каталог $HOME/taglib_build/lib не найден. Возможно, сборка не удалась или установилась в другое место."
            echo "Проверяем возможные альтернативные пути..."
            find $HOME/taglib_build -name "libtag*" 2>/dev/null || echo "Файлы libtag* не найдены в $HOME/taglib_build"
          fi
      # -----------------------
      
      # -----------------------
      # Скачиваем и распаковываем готовую сборку ICU 76.1 для Ubuntu 22.04
      - name: Download and Extract ICU 76.1 (Ubuntu 22.04)
        run: |
          echo "=== Скачивание ICU 76.1 (Ubuntu 22.04) ==="
          wget https://github.com/unicode-org/icu/releases/download/release-76-1/icu4c-76_1-Ubuntu22.04-x64.tgz
          echo "=== Распаковка ICU 76.1 ==="
          tar xzf icu4c-76_1-Ubuntu22.04-x64.tgz
          echo "=== Копирование библиотек ICU 76.1 в AppDir ==="
          mkdir -p AppDir/usr/lib
          # Предполагаем, что библиотеки находятся в подкаталоге lib после распаковки
          echo
          ls -la
          echo
          ls -la ./icu/usr/local/lib/
          echo
          cp ./icu/usr/local/lib/*.* AppDir/usr/lib/
          # Копируем также символьные ссылки .so и основные библиотеки, если они есть в архиве
          # cp icu4c-76_1-Ubuntu22.04-x64/lib/libicui18n.so AppDir/usr/lib/ 2>/dev/null || echo "libicui18n.so (symlink) not found"
          # cp icu4c-76_1-Ubuntu22.04-x64/lib/libicuuc.so AppDir/usr/lib/ 2>/dev/null || echo "libicuuc.so (symlink) not found"
          # cp icu4c-76_1-Ubuntu22.04-x64/lib/libicudata.so AppDir/usr/lib/ 2>/dev/null || echo "libicudata.so (symlink) not found"
    
          echo "Файлы ICU 76 в AppDir/usr/lib/ после копирования:"
          ls -la AppDir/usr/lib/libicu* 2>/dev/null || echo "Файлы libicu* не найдены в AppDir/usr/lib/"
    
          # Удаляем временные файлы
          rm -rf icu4c-76_1-Ubuntu22.04-x64*
      # -----------------------

      # -----------------------
      # 1. Получить последний тег FFmpeg версии 7.x.x
      - name: Get Latest FFmpeg 7.x Tag
        id: ffmpeg-tag
        run: |
          # Клонируем репозиторий FFmpeg в временную директорию
          cd /tmp
          git clone --bare --filter=blob:none --quiet https://git.ffmpeg.org/ffmpeg.git ffmpeg_temp
          cd ffmpeg_temp
          
          # Получаем все теги
          # Фильтруем теги, начинающиеся с 'n7.' (например, n7.0, n7.1)
          # Сортируем по версии и берем последний
          LATEST_TAG=$(git tag -l "n7.*" | sort -V | tail -n 1)
          
          if [ -z "$LATEST_TAG" ]; then
            echo "::error::Не удалось найти тег FFmpeg версии 7.x.x"
            exit 1
          fi
          
          # Получаем хэш коммита для этого тега
          TAG_HASH=$(git rev-parse "$LATEST_TAG")
          
          echo "tag=$LATEST_TAG" >> $GITHUB_OUTPUT
          echo "hash=$TAG_HASH" >> $GITHUB_OUTPUT
          echo "Найден последний тег FFmpeg 7.x.x: $LATEST_TAG ($TAG_HASH)"

      # -----------------------
      # 2. Кешируем сборку FFmpeg из Git
      - name: Cache FFmpeg Git Build
        id: cache-ffmpeg
        uses: actions/cache@v3
        with:
          path: ~/ffmpeg_build
          # Кэш будет промахиваться, если хэш коммита изменится
          key: ffmpeg-git-${{ steps.ffmpeg-tag.outputs.hash }}-${{ runner.os }}

      # -----------------------
      # 3. Сборка FFmpeg из Git, если кеш пустой
      - name: Build FFmpeg from Git
        if: steps.cache-ffmpeg.outputs.cache-hit != 'true'
        run: |
          sudo apt-get update
          sudo apt-get install -y autoconf automake build-essential cmake git libtool \
          pkg-config texinfo yasm nasm zlib1g-dev libx264-dev libx265-dev \
          libvpx-dev libfdk-aac-dev libmp3lame-dev libopus-dev wget

          mkdir -p ~/ffmpeg_build_source && cd ~/ffmpeg_build_source
          git clone https://git.ffmpeg.org/ffmpeg.git
          cd ffmpeg
          
          # Переходим к найденному тегу
          git checkout tags/${{ steps.ffmpeg-tag.outputs.tag }} -b build-branch
          
          # Конфигурация для сборки shared libraries (.so.x)
          # Отключаем статические библиотеки и программы, которые не нужны
          # Отключаем yasm/asm, если возникают ошибки (по аналогии с предыдущими попытками)
          ./configure --prefix=$HOME/ffmpeg_build --disable-programs --disable-doc --enable-shared --disable-static --disable-yasm --disable-asm
          make -j$(nproc)
          make install

      # -----------------------
      # 4. Копируем библиотеки в AppDir и проверяем
      - name: Copy FFmpeg libs to AppDir and verify
        run: |
          echo "=== Копирование библиотек FFmpeg из Git-сборки ==="
          echo "Ищем файлы в $HOME/ffmpeg_build/lib/:"
          ls -la $HOME/ffmpeg_build/lib/libavcodec.so* $HOME/ffmpeg_build/lib/libavformat.so* 2>/dev/null || echo "Файлы .so* не найдены в сборке"
          
          mkdir -p AppDir/usr/lib
          cp $HOME/ffmpeg_build/lib/libavcodec.so* AppDir/usr/lib/
          cp $HOME/ffmpeg_build/lib/libavformat.so* AppDir/usr/lib/
          cp $HOME/ffmpeg_build/lib/*.so* AppDir/usr/lib/
          # cp -r $HOME/ffmpeg_build/include AppDir/usr/ # Обычно не нужно для AppImage
          
          echo "Файлы в AppDir/usr/lib/ после копирования:"
          ls -la AppDir/usr/lib/libavcodec.so* AppDir/usr/lib/libavformat.so* 2>/dev/null || echo "Файлы .so* отсутствуют в AppDir"

      # -----------------------
      # 5. Устанавливаем LD_LIBRARY_PATH в AppRun
      - name: Set library path
        run: |
          echo 'export LD_LIBRARY_PATH=$APPDIR/usr/lib:$LD_LIBRARY_PATH' >> AppDir/AppRun

      - name: Скопировать все зависимости библиотек
        run: |
          copy_deps() {
            local binary="$1"
            if [ ! -f "$binary" ]; then
              echo "❌ Файл не найден: $binary"
              exit 1
            fi
            echo "🔍 Поиск зависимостей для: $binary"
            ldd "$binary" | awk '/=>/ { print $3 }' | while read lib; do
              if [ -n "$lib" ] && [ -f "$lib" ]; then
                mkdir -p "AppDir$(dirname "$lib")"
                cp "$lib" "AppDir$lib"
                echo "   ✅ Скопировано: $lib"
              fi
            done
          }
          copy_deps "AppDir/usr/bin/fooyin"

      - name: Добавить libstdc++ в AppDir
        run: |
          echo "📦 Копирование libstdc++ в AppDir..."
          LIBSTDCPP=$(g++ -print-file-name=libstdc++.so.6)
          if [ -f "$LIBSTDCPP" ]; then
            cp "$LIBSTDCPP" AppDir/usr/lib/
            echo "✅ Скопирован: $LIBSTDCPP"
          else
            echo "❌ Не удалось найти libstdc++"
            exit 1
          fi


      - name: Создать скрипт AppRun
        run: |
          cat > AppDir/AppRun << 'EOF'
          #!/bin/bash
          HERE="$(dirname "$(readlink -f "${0}")")"
          export LD_LIBRARY_PATH="${HERE}/usr/lib/x86_64-linux-gnu:${HERE}/usr/lib:${HERE}/usr/lib/x86_64-linux-gnu/libproxy:${HERE}/lib/x86_64-linux-gnu:${HERE}/lib:${LD_LIBRARY_PATH}"
          export QT_PLUGIN_PATH="${HERE}/usr/lib/x86_64-linux-gnu/qt6/plugins"
          export QML2_IMPORT_PATH="${HERE}/usr/lib/x86_64-linux-gnu/qt6/qml"
          export PATH="${HERE}/usr/bin:${PATH}"
          exec "${HERE}/usr/bin/fooyin" "$@"
          EOF
          chmod +x AppDir/AppRun

      - name: Создать .desktop файл
        run: |
          cat > AppDir/fooyin.desktop << 'EOF'
          [Desktop Entry]
          Name=Fooyin
          Comment=Современный проигрыватель музыки с фокусом на высококачественное воспроизведение аудио
          Exec=fooyin
          Icon=fooyin
          Terminal=false
          Type=Application
          Categories=AudioVideo;Audio;
          MimeType=audio/mpeg;audio/x-flac;audio/wav;audio/x-wav;audio/ogg;audio/x-ogg;audio/aac;audio/x-m4a;audio/flac;audio/mp4;
          EOF

      - name: Скопировать иконку
        run: |
          mkdir -p AppDir/usr/share/icons/hicolor/256x256/apps
          ICON_FILE=$(find AppDir/usr/share/icons -name "*fooyin*" -o -name "*Fooyin*" -type f | head -1)
          if [ -n "$ICON_FILE" ]; then
            cp "$ICON_FILE" AppDir/usr/share/icons/hicolor/256x256/apps/fooyin.png
            cp "$ICON_FILE" AppDir/fooyin.png
            echo "✅ Иконка скопирована: $ICON_FILE"
          else
            echo "⚠️ Иконка не найдена в пакете. Попытка загрузить fallback..."
            wget -O AppDir/fooyin.png https://raw.githubusercontent.com/fooyin/fooyin/master/resources/fooyin-icon.png 2>/dev/null && echo "✅ Fallback иконка загружена" || echo "❌ Не удалось загрузить иконку"
          fi

      - name: Собрать AppImage
        run: |
          ./appimagetool-x86_64.AppImage AppDir Fooyin-${{ env.LATEST_TAG }}-x86_64.AppImage

      - name: Загрузить AppImage как артефакт
        uses: actions/upload-artifact@v4
        with:
          name: Fooyin-AppImage
          path: Fooyin-*.AppImage

      - name: Получить текущую дату
        id: get-date
        run: |
          echo "BUILD_DATE=$(date -u +'%Y-%m-%d %H:%M:%S UTC')" >> $GITHUB_ENV

      - name: Создать GitHub Release (только при тегах)
        #if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v1
        with:
          files: Fooyin-*.AppImage
          tag_name: ${{ env.LATEST_TAG }}
          name: Fooyin ${{ env.LATEST_TAG }}
          body: |
            Fooyin AppImage собран автоматически из последнего .deb пакета.

            **Версия:** ${{ env.LATEST_TAG }}
            **Дата сборки:** ${{ env.BUILD_DATE }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
